@model GradExam.ViewModels.AddLearnOutcomeViewModel

@using (Html.BeginForm("Add","LearnOutcome"))
{
<div class="col-sm-4">
    <div class="form-group">
        @Html.LabelFor(model => model.LearningOutcome, new { style = "width: 160px;" })
        @Html.TextAreaFor(model => model.LearningOutcome, new { rows = "10", cols = "120", @maxlength = "900", style = "width: 900px;", htmlAttributes = new { @class = "form-control" } })
    </div>
    @*<div class="form-group">
        @Html.Label("Question and Answer")
        @Html.DropDownListFor(model => model.QuestionId, new SelectList(@ViewBag.QAs, "Id", "Question"), "--- Q&A ---", new { @class = "form-control" })
    </div>*@
    <button type="submit" class="btn btn-primary">Create Learning Outcome</button>
</div>
}